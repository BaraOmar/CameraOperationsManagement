@model CameraOperationsManagement.ViewModels.RecorderHardDrives.RecorderHardDriveFormViewModel

@{
    ViewData["Title"] = "Edit Hard Drive";
}

<div class="page-header">

    <div>
        <h2 class="page-heading">
            Edit Hard Drive
        </h2>

        <p class="page-description">
            Update the hard drive information.
        </p>
    </div>

</div>


<div class="app-card">

    <div class="app-card-body">

        <form asp-action="Edit"
              asp-route-id="@Model.Id"
              method="post">

            <input asp-for="Id"
                   type="hidden" />

            <input asp-for="RecorderId"
                   type="hidden" />


            <partial name="_HardDriveForm"
                     model="Model" />


            <div class="d-flex gap-2 mt-4">

                <button type="submit"
                        class="btn btn-primary">
                    Save Changes
                </button>

                <a asp-action="Index"
                       asp-route-recorderId="@Model.RecorderId"
                       class="btn btn-outline-secondary">
                    Cancel
                </a>

            </div>

        </form>

    </div>

</div>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}